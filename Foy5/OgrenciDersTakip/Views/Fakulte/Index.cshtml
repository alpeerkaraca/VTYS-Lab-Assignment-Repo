@model IEnumerable<OgrenciDersTakip.Models.TFakulte>
@{
    ViewData["Title"] = "Fakülte Listesi";
}
<h2>Fakülte Listesi</h2>
<p>
    <a class="btn btn-primary" asp-action="Create">Yeni Fakülte Ekle</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Fakülte Adı</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.FakulteAd</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FakulteID" class="btn btn-sm btn-warning me-1"><i class="bi bi-pencil"></i> Düzenle</a>
                <a asp-action="Details" asp-route-id="@item.FakulteID" class="btn btn-sm btn-info me-1"><i class="bi bi-info-circle"></i> Detay</a>
                <a asp-action="Delete" asp-route-id="@item.FakulteID" class="btn btn-sm btn-danger"><i class="bi bi-trash"></i> Sil</a>
            </td>
        </tr>
    }
    </tbody>
</table>